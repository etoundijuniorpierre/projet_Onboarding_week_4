spring.application.name=checkpoint-service
server.port=8082

# Datasource
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.url=jdbc:postgresql://localhost:5432/checkpointdb

# JPA
spring.jpa.database=postgresql
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Liquibase
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.yaml
spring.liquibase.enabled=true
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Configuration Axon
axon.axonserver.servers=axon-server:8124
axon.eventhandling.processors.checkpointEvents.mode=subscribing